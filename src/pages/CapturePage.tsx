import { CaptureForm } from '@/components/CaptureForm';

export default function CapturePage() {
  return (
    <div className="pb-20 px-4 pt-6 max-w-lg mx-auto">
      <h1 className="text-2xl font-semibold mb-6">Capture</h1>
      <CaptureForm />
    </div>
  );
}
